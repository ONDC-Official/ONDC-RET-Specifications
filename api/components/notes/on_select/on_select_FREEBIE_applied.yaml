value:
  [^1]: may be the same location as selected by the buyer or a different location
  [^2]: offer quote construct to be added in /on_init, /on_confirm, /on_status, /on_cancel, /on_update
  [^3]: " based on quote type, item_id to be assigned as follows:
          "order" - transaction id;
          "fulfillment" - fulfillment id;
          "item" - item id (for dynamic items, the entire quote construct for dynamic items should be added)"
  [^4]: quote type for title type above (mapping of title_type to quote type defined [here](https://docs.google.com/document/d/1brvcltG_DagZ3kGr1ZZQk4hG4tze3zvcxmGV4NMTzr8/edit#heading=h.g252ed4gpeis))
  [^5]: enum - "order", "fulfillment", "item"
  [^6]: offer type (with enum) as per offer template above
  [^7]: whether offer is auto applied based on cart selection
  [^8]: enum - "yes", "no"
  [^9]: whether offer can be applied in addition to other offers
  [^10]: enum - "yes", "no"
  [^11]: optional - to be provided if required for offer type as per template definition
  [^12]: optional - to be provided if required for offer type as per template definition
  [^13]: optional - to be provided if required for offer type as per template definition