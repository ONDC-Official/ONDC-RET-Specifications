tags:
  veg_nonveg:
   descriptor:
    code:
      - code: veg
        description: if the item is veg, value = yes, either of veg, non_veg to be used
        reference: <PR/Issue/Discussion Links md format text>
      - code: non_veg
        description: if the item is non-veg, value = yes, either of veg, non_veg to be used
        reference: <PR/Issue/Discussion Links md format text>

