message:
  parent: true
  order:
    parent: true
    quote:
      parent: true
      quote:
        description: quote to be sent from seller NP
        required: required
        usage: "-"
        reference: if any
      breakup:
        breakup:
          description: Seller NP provides the the breakup of the quote
          required: required
          usage: "-"
          reference: if any
        "@ondc/org/title_type":
          description: type of charge - item, packing,discount,tax,misc,delivery
          required: required
          usage: "delivery"
          reference: if any
    payments:
      parent: true
      "@ondc/org/buyer_app_finder_fee_type": 
        description: buyer app finder fee type - percent,amount
        required: required
        usage: "percent"
        reference: if any
      "@ondc/org/buyer_app_finder_fee_amount":
        description: amount of buyer app finder fee (either in percent or actual amount)
        required: required
        usage: "3"
        reference: if any
      "@ondc/org/settlement_details":
        description: Details of counter party for settling the money
        required: required
        usage: "-"
        reference: if any 
